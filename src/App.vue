<script setup>
// Root component.
// Layouts (navbar + footer + preloader) are applied per-route via
// `layouts/DefaultLayout.vue` and the router — see `src/router/index.js`.
</script>

<template>
  <router-view />
</template>
